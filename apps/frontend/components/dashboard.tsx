export function Dashboard() {
  return (
    <section className="grid">
      <article className="card">
        <h3>Backend API Status</h3>
        <p>Frontend pages now consume backend API endpoints for games and recommendations.</p>
        <span className="tag">API-backed</span>
      </article>
    </section>
  );
}
